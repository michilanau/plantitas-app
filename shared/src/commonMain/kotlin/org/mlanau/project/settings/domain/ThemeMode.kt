package org.mlanau.project.settings.domain

/**
 * How the app decides between its light and dark palettes. [SYSTEM] follows the device setting and
 * is the default, matching what most apps do today.
 */
enum class ThemeMode { LIGHT, DARK, SYSTEM }
